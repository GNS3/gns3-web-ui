export interface TracengTemplate {
  builtin: boolean;
  category: string;
  compute_id: string;
  console_type: string;
  default_name_format: string;
  ip_address: string;
  name: string;
  symbol: string;
  template_id: string;
  template_type: string;
}
