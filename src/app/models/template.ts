export class Template {
  template_id: string;
  builtin: boolean;
  category: string;
  compute_id: string;
  default_name_format: string;
  name: string;
  node_type: string;
  symbol: string;
  template_type: string;
}
