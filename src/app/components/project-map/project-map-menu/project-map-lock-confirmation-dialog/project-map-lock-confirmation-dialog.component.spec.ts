import { ProjectMapLockConfirmationDialogComponent } from './project-map-lock-confirmation-dialog.component';
import { describe, it, expect } from 'vitest';

describe('ProjectMapLockConfirmationDialogComponent', () => {
  it('should be defined', () => {
    expect(ProjectMapLockConfirmationDialogComponent).toBeDefined();
  });

  it('should have correct component name', () => {
    expect(ProjectMapLockConfirmationDialogComponent.name).toBe('ProjectMapLockConfirmationDialogComponent');
  });
});
