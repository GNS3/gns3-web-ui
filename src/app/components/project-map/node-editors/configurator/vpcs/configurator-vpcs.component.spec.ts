import { ConfiguratorDialogVpcsComponent } from './configurator-vpcs.component';
import { describe, it, expect } from 'vitest';

describe('ConfiguratorDialogVpcsComponent', () => {
  it('should be defined', () => {
    expect(ConfiguratorDialogVpcsComponent).toBeDefined();
  });

  it('should have correct component name', () => {
    expect(ConfiguratorDialogVpcsComponent.name).toBe('ConfiguratorDialogVpcsComponent');
  });
});
