import { ConfiguratorDialogAtmSwitchComponent } from './configurator-atm-switch.component';
import { describe, it, expect } from 'vitest';

describe('ConfiguratorDialogAtmSwitchComponent', () => {
  it('should be defined', () => {
    expect(ConfiguratorDialogAtmSwitchComponent).toBeDefined();
  });

  it('should have correct component name', () => {
    expect(ConfiguratorDialogAtmSwitchComponent.name).toBe('ConfiguratorDialogAtmSwitchComponent');
  });
});
