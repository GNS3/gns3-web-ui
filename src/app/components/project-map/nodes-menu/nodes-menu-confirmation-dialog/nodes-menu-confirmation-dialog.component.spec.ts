import { NodesMenuConfirmationDialogComponent } from './nodes-menu-confirmation-dialog.component';
import { describe, it, expect } from 'vitest';

describe('NodesMenuConfirmationDialogComponent', () => {
  it('should be defined', () => {
    expect(NodesMenuConfirmationDialogComponent).toBeDefined();
  });

  it('should have correct component name', () => {
    expect(NodesMenuConfirmationDialogComponent.name).toBe('NodesMenuConfirmationDialogComponent');
  });
});
